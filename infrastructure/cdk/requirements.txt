# CDK Infrastructure Requirements
# Pinned to exact versions to prevent future breakage.
#
# Compatibility note: aws-cdk-lib and aws-solutions-constructs must use the
# same aws-cdk.cloud-assembly-schema version range. These versions were the
# latest available on Feb 18, 2026 (the last known-good deploy date) and are
# mutually compatible:
#   aws-cdk-lib==2.238.0            -> cloud-assembly-schema >=48.20.0,<49.0.0
#   aws-solutions-constructs==2.99.0 -> cloud-assembly-schema >=48.6.0,<49.0.0
# DO NOT upgrade aws-cdk-lib without also upgrading aws-solutions-constructs
# to a matching version (same minor series).

aws-cdk-lib==2.238.0
constructs==10.5.1
aws-solutions-constructs.aws-alb-fargate==2.99.0
aws-solutions-constructs.aws-cloudfront-s3==2.99.0
aws-solutions-constructs.aws-cloudfront-apigateway==2.99.0
boto3==1.42.63
